import React from 'react';

const Loading = () => {
	return (
		<div className='flex justify-center items-center min-h-screen'>
			<button className="btn loading btn-accent text-white btn-wide font-bold ">Loading</button>
		</div>
	);
};

export default Loading;